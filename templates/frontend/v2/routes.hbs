import { Routes } from '@angular/router';

export const {{moduleName}}Routes: Routes = [
  {
    path: '',
    loadComponent: () =>
      import('./components/{{kebabCaseHelper moduleName}}-list.component').then(
        (m) => m.{{pascalCaseHelper moduleName}}ListComponent
      ),
    title: '{{title}}'
  }
];